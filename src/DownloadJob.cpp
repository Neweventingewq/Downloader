#include "DownloadJob.h"

// All members are POD / Qt value types; nothing to implement here.
// The .cpp file exists so the type can be registered with Qt's
// metatype system from main() if we ever need to send DownloadJob
// across thread boundaries.
